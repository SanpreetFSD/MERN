// first we have to define the function

// function sum(){
//     var a = 10, b = 20;
//     var total = a+b;
//     console.log (total);
// }

// calling a function

// sum();

/*function sum(){
    var a = 10, b = 550;
    var total = (a+b); // the total value can work with or without ()
    console.log (total);
}

sum();*/

function sum(a,b){          // in function declaration ()"parenthesis" what we pass is called parameters
    //var a = 10, b = 550;
    var total = (a+b); // the total value can work with or without ()
    console.log (total);
}

// sum();
sum(45,20);                 //in function calling ()"parenthesis" what we pass is called argument
sum(25,33);