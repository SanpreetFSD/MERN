// Anonymous function expression and anonymous function

function sum (a,b){
    return total = a+b;
}

var funExp = sum(10,35);

var funExp1 = sum(20, 40)

console.log("The sum of numbers is " +funExp);
console.log("The sum of numbers is " +funExp1);  // this way we can use the function many times

console.log(funExp > funExp1);  // we can also check the greater than and less than