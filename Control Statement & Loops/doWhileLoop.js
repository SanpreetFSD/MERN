var num = 0;

do{
    console.log(num);
    num++;
}while(num <= 15);  // do while loop checks the condition at last and gives the output before


// The initialization of the variable may occur within or before the execution of the loop. 
//The while loop is an entry-controlled type of loop. The do-while loop is an exit-controlled type of loop.